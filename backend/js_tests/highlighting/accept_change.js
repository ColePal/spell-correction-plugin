QUnit.module('highlighting', {
    //set up the map, the previous sent, all that jazz
});

QUnit.test('User accepts changes in order', function(assert) {
    assert.equal()
});

QUnit.test('User accepts first and last change', function(assert) {

});

QUnit.test('User accepts last and first change', function(assert) {

});